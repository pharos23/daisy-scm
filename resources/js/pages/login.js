// JS File to call the scripts needed for this page (in this case: login and register pages)
import { setupThemeToggle } from "../utils/themeToggle.js";

document.addEventListener('DOMContentLoaded', () => {
    setupThemeToggle()
});
