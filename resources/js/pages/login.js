// JS file to initialize scripts for the Login and Register pages

// Import the function that handles theme switching (e.g., light/dark mode)
import { setupThemeToggle } from "../utils/themeToggle.js";

// Once the DOM is ready, activate the theme toggle functionality
document.addEventListener('DOMContentLoaded', () => {
    setupThemeToggle()// JS File to call the scripts needed for this page (in this case: login and register pages)

});
