// JS File to call the scripts needed for this page (in this case: home/dashboard page)
import {updateWaitWheel} from "../event-handler/updateWaitWheel.js";

document.addEventListener('DOMContentLoaded', () => {
    updateWaitWheel();
});
